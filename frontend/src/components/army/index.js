import React from 'react'
import styled from '@emotion/styled'

import { flexColCss } from '../styled'

const ArmyWrapper = styled('div')`
  ${flexColCss}
`

const Army = () => (
  <ArmyWrapper />
)

export default Army
